# Example Module

This fixture proves that a module can be described, checked, and removed without changing the core application.

It intentionally contains no business routes or persistence. Use it as the input contract for `scripts/make-module.sh`.
