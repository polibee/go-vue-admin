# Example Module Acceptance

- The module id is `example`.
- `module.yaml`, `README.md`, and `acceptance.md` are present.
- The backend and admin boundaries contain no imports into the core application.
- `scripts/module-check.sh modules/example` completes successfully.
