# Go Vue Admin Developer Docs

This is a standalone static developer documentation site.

Build it from the repository root:

    node docs-site/scripts/build.mjs

The generated site is written to docs-site/dist and is not consumed by the
admin application or backend.

The root site is English by default. Simplified Chinese is available at
docs-site/dist/zh-CN after building.
