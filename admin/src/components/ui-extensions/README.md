# UI extensions

Add a component here only when an official shadcn-vue component and a
reasonable composition cannot satisfy the requirement. Document the reason
and keep business behavior outside the upstream UI directories.
